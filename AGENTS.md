When changing data-processing code, avoid hidden quadratic behavior and repeated expensive conversions.

- Store derived metadata on temporary objects instead of recomputing it.
- Keep transactions short and DB-focused.
